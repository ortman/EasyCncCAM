#ifndef __GCODE_GENERATOR__
#define __GCODE_GENERATOR__

#include <Core/Core.h>

using namespace Upp;

class GCodeGenerator {
public:
	static StringStream Generate(const Vector<Operation*> &operations) {
	}
};

#endif